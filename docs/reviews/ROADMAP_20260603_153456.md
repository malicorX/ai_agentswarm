# AgentSwarm � Evidence-Based Project Review

**Review date (local):** 2026-06-03 15:34:56  
**Reviewer role:** Senior project reviewer + roadmap architect  
**Repository path:** `m:\Data\Projects\ai_agentSwarm`  
**HEAD / VCS:** No git repository present (verified 2026-06-03)

---

## 1. Executive Summary

**Current health: Critical gap between ambition and execution.** The project is a **concept-stage specification** with **zero application code**, **no version control**, **no tests**, and **no runnable platform or pilot product**. The only durable artifacts are `ROADMAP.md` (34,498 bytes) and an archived PDF copy under `docs/archive/`.

**Biggest strengths:** `ROADMAP.md` is unusually thorough for a pre-MVP effort. It articulates a coherent long-term vision (federated volunteer compute, pull-based tasks, earned credibility, human supervision), names a concrete pilot (AI News Hub), defines 22 agent types, sketches a pull-based protocol, layers security mitigations, and stages delivery in Phases 0�4. The document is internally consistent on core principles (�1.1, �4, �11).

**Biggest risks:**
1. **Documentation-as-progress trap** � 551 lines of design can be mistaken for momentum while Phase 0 has not started.
2. **Scope inflation before MVP** � Later-phase concepts (federation, gamification, shared memory, N-way replication, economics) are specified in detail before any task pool exists.
3. **No engineering substrate** � No git, README, ADRs, CI, or reference implementation means the next contributor cannot verify claims or run anything.
4. **Phase 0 vs. �4 tension** � Phase 0 claims �structured for �4 from day one,� but �4.3 lists SDK, container image, and dashboard that Phase 0 does not include; this invites over-building or false �done� states.
5. **Open decisions blocking implementation** � Credibility math, identity model, and MCP vs. custom protocol remain unresolved (�16) while a custom REST-like protocol is already sketched (�6.2).

**On track toward long-term goals?** **No � not yet.** The **direction** in documentation aligns with stated goals, but **implementation has not begun**. The project is **not advancing** toward a self-sustaining swarm; it is **planning only**. That is appropriate for �Concept / pre-MVP� (line 3 of `ROADMAP.md`) but is a material schedule risk if stakeholders interpret the roadmap as delivery progress.

---

## 2. Review Method

### Inspected (with evidence)

| Area | Method | Result |
|------|--------|--------|
| Full file tree | `Get-ChildItem -Recurse -Force` on repo root | **4 paths:** `ROADMAP.md`, `docs/`, `docs/archive/`, `docs/archive/AgentSwarm_Roadmap_final_download.pdf` |
| Markdown / docs | Read `ROADMAP.md` in full (551 lines); glob `**/*.{md,modelmd}` | **1** markdown file; **0** `*.modelmd` files |
| Source code | Glob `**/*`; grep all files | **No** `.py`, `.ts`, `.js`, `.go`, `.rs`, etc. |
| Tests | Search for `test/`, `*_test.*`, config files | **None** |
| Config / tooling | Explicit existence check for `package.json`, `pyproject.toml`, `docker-compose.yml`, `Makefile`, `README.md`, `AGENTS.md`, `.gitignore` | **All absent** |
| Git / HEAD alignment | `git status`, `git log` | **`fatal: not a git repository`** |
| `modelmd` / `./modelmd` | Case-insensitive grep `modelmd` | **No matches** |
| Prior review docs | Glob for `*review*`, `*ADR*`, `runbook*` | **None** (besides this file, created by this review) |
| PDF archive | Path + size + timestamp only | Present; **content not diffed** against `ROADMAP.md` (no PDF tooling run) |

### Commands run (exact)

```powershell
Get-ChildItem -Recurse -Force "m:\Data\Projects\ai_agentSwarm"
git status   # in m:\Data\Projects\ai_agentSwarm ? fatal: not a git repository
Get-Date -Format "yyyyMMdd_HHmmss"   # ? 20260603_153456
where.exe markdownlint   # not found
where.exe npx            # found (Node present; markdownlint not invoked � no project package.json)
```

### Not reviewed (explicit gaps)

- **PDF body text** � `docs/archive/AgentSwarm_Roadmap_final_download.pdf` (146,078 bytes): not parsed; staleness vs. `ROADMAP.md` is **unverified**.
- **External dependencies** � No lockfiles exist; nothing to audit.
- **Runtime behavior** � No services to run or profile.
- **Security penetration** � No deploy surface; threat model in `ROADMAP.md` �11 is **design-only**.
- **Agent transcripts / prior chats** � Out of repo scope.
- **Network / production infrastructure** � None declared.

### Assumptions (marked)

- **Assumption:** `ROADMAP.md` on disk is the authoritative spec (no other branch or remote).
- **Assumption:** User query reference to `./modelmd` means markdown documentation sprawl (`.md`); no `modelmd` extension exists in repo.
- **Open question:** Whether the PDF is an older export or a duplicate of current `ROADMAP.md`.

---

## 3. Strong Parts

### 3.1 Vision and design principles are clear and ordered

`ROADMAP.md` �1��1.1 states goals and six ordered principles (lightweight center, pull model, earned trust, multi-signature verification, openness + signing, human supervision). These give a decision filter for trade-offs.

**Evidence:** `ROADMAP.md` lines 7�24.

### 3.2 Pilot project is well chosen

AI News Hub exercises content agents (scraper, summarizer) and engineering agents (codewriter, tester, deployer), matching the 22-type taxonomy in �5.

**Evidence:** `ROADMAP.md` lines 28�43, 134�176.

### 3.3 Distributed compute model is concrete

BOINC analogy (�4.1) maps cleanly to volunteer agent compute; �4.2�4.4 separate platform vs. owner responsibilities and avoid shared API keys (�4.4, �11.3).

**Evidence:** `ROADMAP.md` lines 90�130, 359�361.

### 3.4 Protocol sketch is implementable as a starting point

�6.2 defines `register`, `poll_tasks`, `claim`, `checkpoint`, `submit`, `poll_verifications`, `verify` � sufficient for an OpenAPI-first Phase 0 service.

**Evidence:** `ROADMAP.md` lines 191�214.

### 3.5 Security is threat-driven, not checklist-driven

�11.5 lists attacker patterns (Sybil rings, canaries, token parasite, rubber-stamping) with named mitigations � good input for acceptance tests later.

**Evidence:** `ROADMAP.md` lines 345�378.

### 3.6 Phased delivery is intellectually honest about staging

Phases 0�4 (�17) defer federation and full self-orchestration; Phase 0 scope is narrower than the full vision.

**Evidence:** `ROADMAP.md` lines 472�526.

### 3.7 Open questions are documented

�16 lists unresolved items (MCP vs. custom, credibility math, quorum sizes) instead of hiding uncertainty.

**Evidence:** `ROADMAP.md` lines 455�468.

---

## 4. Weak Parts / Risks

Severity: **Critical (C)** | **High (H)** | **Medium (M)** | **Low (L)**

---

### C1 � No implementation exists despite Phase 0-defined MVP

| Field | Detail |
|-------|--------|
| **Problem** | Zero source files, services, or agents; Phase 0 deliverables (task pool, audit log, three reference agents, AI News Hub scaffold) are **0% built**. |
| **Why it matters** | Long-term goal requires a running loop: claim ? work ? submit ? verify. Without it, the project cannot learn or falsify design assumptions. |
| **Evidence** | Full tree listing (�2); `ROADMAP.md` �17 Phase 0 lines 476�485. |
| **Suggested fix** | Execute **Package P0** (git + monorepo scaffold) then **P1�P3** (minimal platform + one agent). |
| **Risk if ignored** | Roadmap remains fiction; contributor onboarding fails; design drift accumulates without code feedback. |

---

### C2 � No version control or contributor entrypoint

| Field | Detail |
|-------|--------|
| **Problem** | No `.git`, no `README.md`, no `CONTRIBUTING.md`, no license file observed. |
| **Why it matters** | Cannot review �HEAD,� branch, PR, or attribute changes; blocks multi-agent and human collaboration. |
| **Evidence** | `git status` ? `fatal: not a git repository`; standard files check all `Exists = False`. |
| **Suggested fix** | Package P0: `git init`, `.gitignore`, `README.md` with honest status, license choice. |
| **Risk if ignored** | No reproducible baseline for future reviews or agent packages. |

---

### H1 � Phase 0 scope ambiguity vs. �4 �from day one�

| Field | Detail |
|-------|--------|
| **Problem** | Phase 0 says platform �structured for �4 from day one� (line 478), but �4.3 requires SDK (Python + TS), reference container, and public dashboard � none listed in Phase 0 bullets. |
| **Why it matters** | Teams either over-build Phase 0 or ship a local script stack that cannot grow into distributed agents without rewrite. |
| **Evidence** | `ROADMAP.md` lines 115�121 vs. 476�485. |
| **Suggested fix** | Add `docs/adr/0001-phase0-scope.md`: Phase 0 = HTTP task API + signed audit log + 3 in-process agents; **explicitly defer** SDK, dashboard, sandbox to Phase 1/2. Amend `ROADMAP.md` �17 in a human-reviewed edit. |
| **Risk if ignored** | Phase 0 never completes or ships a non-pull architecture. |

---

### H2 � Implicit task chaining not in Phase 0 plan

| Field | Detail |
|-------|--------|
| **Problem** | �3.3 states each `codewriter` task �implicitly creates downstream `tester` and `reviewer` tasks� (line 82); Phase 0 only lists three hand-built agents, not orchestration rules. |
| **Why it matters** | Core swarm loop may be manual forever unless task-creation rules are specified early. |
| **Evidence** | `ROADMAP.md` lines 82, 476�485. |
| **Suggested fix** | Phase 0: document **manual** task creation by maintainer OR minimal `on_submit ? enqueue verification tasks` rule in task pool. |
| **Risk if ignored** | Codewriter submissions pile up unverified; credibility mechanics (Phase 2) lack substrate. |

---

### H3 � Credibility and verification designed before minimal ledger

| Field | Detail |
|-------|--------|
| **Problem** | �9��10 describe minting, decay, N-way replication, canaries; �16 admits credibility math is unspecified. |
| **Why it matters** | Implementing credibility before basic accept/reject invites rework; skipping it entirely leaves Phase 0 without trust model. |
| **Evidence** | `ROADMAP.md` lines 279�341, 459�462, 498�507. |
| **Suggested fix** | Phase 0: binary accept/reject + audit log only; Phase 2: numeric credibility per �9 with simulated parameters. |
| **Risk if ignored** | Premature complexity or permanent �trust everyone� closed swarm. |

---

### H4 � MCP vs. custom protocol undecided but custom API sketched

| Field | Detail |
|-------|--------|
| **Problem** | �16 lists �MCP vs. custom protocol� as open; �6.2 already specifies custom HTTPS endpoints. |
| **Why it matters** | Wrong bet wastes SDK work or blocks tool ecosystem integration. |
| **Evidence** | `ROADMAP.md` lines 459, 191�219. |
| **Suggested fix** | Time-boxed spike (Package P8): MCP mapping for `poll_tasks` / `submit` vs. REST; record ADR. |
| **Risk if ignored** | Contributors build against REST; later MCP migration breaks clients. |

---

### M1 � Documentation duplicate (PDF archive)

| Field | Detail |
|-------|--------|
| **Problem** | `docs/archive/AgentSwarm_Roadmap_final_download.pdf` may duplicate `ROADMAP.md`; if diverged, misleads readers. |
| **Why it matters** | Two �roadmaps� without provenance creates authority confusion. |
| **Evidence** | File listing; PDF not diffed. |
| **Suggested fix** | Archive with `docs/archive/README.md` stating �export of ROADMAP as of DATE� or delete after hash/text compare. |
| **Risk if ignored** | Stakeholders cite outdated PDF decisions. |

---

### M2 � Contributing section presumes non-existent swarm process

| Field | Detail |
|-------|--------|
| **Problem** | �19 says changes arrive via `documenter`/`planner` submission and agent review � no agents, no task pool. |
| **Why it matters** | New contributors follow impossible process. |
| **Evidence** | `ROADMAP.md` lines 548�550. |
| **Suggested fix** | Until Phase 1: �Changes via PR + human maintainer� in `CONTRIBUTING.md`. |
| **Risk if ignored** | Confusion and abandoned contributions. |

---

### M3 � 22 agent types vs. Phase 0 three agents

| Field | Detail |
|-------|--------|
| **Problem** | Full taxonomy may encourage building scrapers/summarizers before closed-loop codewriter/tester/reviewer works. |
| **Why it matters** | Pilot breadth (AI News Hub) tempts parallel workstreams without platform. |
| **Evidence** | `ROADMAP.md` �5 vs. �17 Phase 0. |
| **Suggested fix** | Tag types in docs as `P0` / `P1` / `pilot-only` / `later`. |
| **Risk if ignored** | Fragmented agents that never integrate. |

---

### L1 � No `README.md` at repo root

| Field | Detail |
|-------|--------|
| **Problem** | GitHub/GitLab default view empty; status buried in roadmap line 3. |
| **Evidence** | File check. |
| **Suggested fix** | Short README: vision one-liner, link to `ROADMAP.md`, �Status: Phase 0 not started.� |
| **Risk if ignored** | Poor first impression for recruits. |

---

### L2 � No `modelmd` files found (query scope)

| Field | Detail |
|-------|--------|
| **Problem** | User asked to audit `./modelmd` fragmentation � **none exist**. |
| **Evidence** | Grep `modelmd` ? no matches; only `ROADMAP.md`. |
| **Suggested fix** | Apply consolidation rules to `.md` sprawl **when** docs multiply; see �7. |
| **Risk if ignored** | N/A today; future sprawl if every agent session writes a new top-level md. |

---

## 5. Long-Term Goal Alignment

**Stated long-term goal (summary):** Open federated platform where independent signed agents pull tasks, earn credibility through verification, collaborate on shared software (pilot: AI News Hub), with human supervision and BOINC-style redundancy (`ROADMAP.md` �1, �4, �17 Phase 4).

### Clearly aligned work

| Item | Alignment |
|------|-----------|
| `ROADMAP.md` vision, principles, pilot choice | Matches long-term platform + product goals |
| Pull-based protocol sketch (�6.2) | Matches �lightweight center, heavy edges� |
| Security layering (�11) | Matches trust-earned and abuse prevention goals |
| Phased roadmap (�17) | Correct sequencing *on paper* |

### Partially aligned work

| Item | Gap |
|------|-----|
| Detailed �9��15 (credibility, gamification, economics) | Aligned **intent** but **premature** without Phase 0 loop |
| AI News Hub as pilot | Aligned **if** platform ships first; building site alone without task pool is **partial** |
| �8 shared memory | Phase 3; documenting now is fine, building now would distract |

### Misaligned or distracting work (if started now)

| Item | Why |
|------|-----|
| Phase 4 federation | No single-project swarm exists |
| Full dashboard / gamification (�13, �15) | No tasks to display |
| 22 agent types in parallel | Violates Phase 0 �closed trusted swarm� |
| PDF as second roadmap without labeling | Authority drift |

### Missing foundational work (required for alignment)

1. Git repository + README + license  
2. Monorepo layout: `platform/`, `agents/`, `pilot/news-hub/` (or equivalent)  
3. Task pool MVP + append-only signed audit log  
4. One end-to-end demo: create task ? claim ? submit ? verify ? log entry  
5. ADR for Phase 0 scope boundary and identity/credibility deferrals  
6. CI running lint + unit tests on platform package  

**Verdict:** Documentation **describes** the right north star; repository **does not move** toward it yet.

---

## 6. Documentation Accuracy Review

| Document | Status | Evidence | Recommended action |
|----------|--------|----------|---------------------|
| `ROADMAP.md` | **Current** (self-consistent); status line accurate (�Concept / pre-MVP�) | Line 3; no code contradicts | Keep as **primary spec**; split execution tracking to `docs/status.md` later |
| `docs/archive/...pdf` | **Unknown / possibly duplicative** | Not text-compared | Archive with README + date; or remove after verify |
| `README.md` | **Missing** | File check | Create (Package P0) |
| `CONTRIBUTING.md` | **Missing** | � | Create; override �19 until swarm exists |
| ADRs / decision logs | **Missing** | � | Add `docs/adr/` starting with Phase 0 scope |
| Runbooks | **Missing** | � | Defer to first deployable service |
| Tests docs | **Missing** | No tests | Add when Package P2 exists |
| Prior review docs | **N/A** | None before this file | This file becomes review baseline |
| `*.modelmd` | **N/A** | 0 files | No action |
| `ROADMAP_20260603_153456.md` (this file) | **Current as of review timestamp** | Created 2026-06-03 | Use for execution planning; not a replacement for product spec |

**HEAD accuracy note:** There is **no git HEAD**. �Accuracy against HEAD� reduces to **accuracy against filesystem snapshot** on 2026-06-03: `ROADMAP.md` correctly describes a project with no implementation.

---

## 7. Markdown Consolidation Guide

### 7.1 Authoritative sources (keep)

| File | Role |
|------|------|
| `ROADMAP.md` | Product + architecture spec (vision through Phase 4) |
| `README.md` | *(to create)* Entry point, status, quick links |
| `docs/adr/*.md` | *(to create)* Decision records that amend or narrow the roadmap |
| `docs/status.md` | *(optional)* Phase checklist updated per milestone � keeps `ROADMAP.md` stable |

### 7.2 Merge candidates

| Sources | Into | When |
|---------|------|------|
| `ROADMAP.md` �17 checklists + execution notes | `docs/status.md` | When phase tracking grows noisy in main roadmap |
| Duplicate roadmap content in PDF | `docs/archive/` only, with pointer in README | After confirming PDF ? markdown |

### 7.3 Archive (do not delete without review)

| File | Action |
|------|--------|
| `docs/archive/AgentSwarm_Roadmap_final_download.pdf` | Keep in archive; add `docs/archive/README.md` with export date and �superseded by ROADMAP.md if newer� |

### 7.4 Delete only after human review

- PDF � only if byte-identical or strictly older than `ROADMAP.md` and redundant  
- Any future session-generated `ROADMAP_*.md` review snapshots � retain latest + milestones; prune duplicates older than N reviews if redundant

### 7.5 Suggested documentation structure

```text
/
??? README.md                 # Status, how to run, links
??? ROADMAP.md                # Stable product/architecture spec
??? CONTRIBUTING.md           # Human process until swarm automation
??? LICENSE
??? docs/
?   ??? status.md             # Phase 0�4 checklist (living)
?   ??? adr/
?   ?   ??? 0001-phase0-scope.md
?   ?   ??? 0002-identity-model.md
?   ?   ??? 0003-protocol-rest-vs-mcp.md
?   ??? protocol/
?   ?   ??? openapi.yaml      # Generated/ maintained from �6.2
?   ??? reviews/
?   ?   ??? ROADMAP_YYYYMMDD_HHMMSS.md
?   ??? archive/
?       ??? README.md
?       ??? AgentSwarm_Roadmap_final_download.pdf
```

### 7.6 Rules to prevent documentation sprawl

1. **One spec:** `ROADMAP.md` changes only for strategic/architectural shifts; not for weekly status.  
2. **ADRs for decisions:** Anything that answers �16 open questions gets an ADR, not a new top-level md.  
3. **Reviews are dated snapshots:** `docs/reviews/ROADMAP_*` � do not fork product spec per review.  
4. **No root-level essay files** from agents without PR review.  
5. **PDF exports** go only under `docs/archive/` with README metadata.  
6. **Reject `modelmd` or duplicate extensions** � standardize on `.md`.  
7. **Code is truth for behavior:** OpenAPI + tests override prose when they diverge; fix docs in same PR as code.

---

## 8. Recommended Work Packages

Packages are ordered by dependency. **Human review before merge** marked **Yes** where process/bootstrap/legal applies.

---

### Package P0 � Repository bootstrap

| Field | Value |
|-------|--------|
| **Goal** | Establish version control and contributor entrypoint |
| **In scope** | `git init`, `.gitignore`, `README.md`, `LICENSE`, `CONTRIBUTING.md` (human-maintainer process) |
| **Out of scope** | Application code, platform service |
| **Steps** | 1. Choose license (MIT/Apache-2.0). 2. `git init`. 3. `.gitignore` for OS/IDE/env. 4. README: vision, honest �Phase 0 not started,� link `ROADMAP.md`. 5. CONTRIBUTING: PR + maintainer; note �19 deferred. 6. Initial commit. |
| **Verification** | `git status`; `git log -1` |
| **Acceptance** | Cloneable repo with README explaining status |
| **Risk** | Low |
| **Human review** | **Yes** (license + contribution policy) |

---

### Package P1 � Monorepo layout + ADR Phase 0 scope

| Field | Value |
|-------|--------|
| **Goal** | Physical structure matching phased plan; narrow Phase 0 |
| **In scope** | `platform/`, `agents/`, `pilot/news-hub/`, `docs/adr/0001-phase0-scope.md`, `docs/status.md` |
| **Out of scope** | Full SDK, dashboard, credibility math |
| **Steps** | 1. Create directories with placeholder READMEs. 2. ADR: Phase 0 = REST task API + audit log + 3 agents in-process; defer �4.3 SDK/container/dashboard. 3. `docs/status.md` checklist from �17 Phase 0. |
| **Verification** | Tree matches layout; ADR linked from README |
| **Acceptance** | ADR explicitly resolves H1 scope tension |
| **Risk** | Low |
| **Human review** | **Yes** (ADR approval) |

---

### Package P2 � Task pool MVP + signed audit log

| Field | Value |
|-------|--------|
| **Goal** | Minimal pull-based loop per �6.2 |
| **In scope** | `platform/` service: `POST /tasks`, `GET /tasks/poll`, `POST /tasks/{id}/claim`, `POST /tasks/{id}/submit`, append-only audit log, Ed25519 verify stub; OpenAPI in `docs/protocol/openapi.yaml` |
| **Out of scope** | Credibility scores, N-way replication, GitHub OAuth |
| **Steps** | 1. Pick stack (recommend Python FastAPI or Node Fastify � **ADR required**). 2. Implement in-memory or SQLite store. 3. Task lifecycle: created ? claimable ? claimed ? submitted ? verified. 4. Signatures: verify agent pubkey on submit (keys registered manually in Phase 0). 5. Audit log: JSON lines, hash-chained optional. 6. Unit tests for state transitions. |
| **Verification** | `pytest` or `npm test`; `curl` script for happy path |
| **Acceptance** | Script demonstrates create ? claim ? submit ? verify with log entry |
| **Risk** | Medium |
| **Human review** | **Yes** (API shape + crypto choices) |

---

### Package P3 � Reference agents (codewriter, tester, reviewer)

| Field | Value |
|-------|--------|
| **Goal** | Three hand-built agents against P2 API (Phase 0) |
| **In scope** | `agents/codewriter`, `agents/tester`, `agents/reviewer` � poll/claim/submit; trivial task types (e.g. �add file comment�, �run pytest�, �approve/reject�) |
| **Out of scope** | LLM integration (can be stub); container image |
| **Steps** | 1. Shared thin client lib in `agents/common`. 2. Each agent: register key, poll filter by capability, claim, execute, sign submit. 3. Tester auto-creates verification task OR maintainer script enqueues (document choice). 4. Integration test: all three on localhost. |
| **Verification** | `docker compose up` or single script `scripts/demo_phase0.sh` |
| **Acceptance** | One codewriter task flows to tester + reviewer without manual copy-paste |
| **Risk** | Medium |
| **Human review** | Recommended |

---

### Package P4 � AI News Hub scaffold (pilot shell)

| Field | Value |
|-------|--------|
| **Goal** | Target system repo area per Phase 0 |
| **In scope** | `pilot/news-hub/` minimal static site or Next.js shell; README; tasks can target files here |
| **Out of scope** | Scrapers, production deploy |
| **Steps** | 1. Scaffold site. 2. Define 2�3 codewriter tasks (e.g. landing page, layout). 3. Run through P3 agents. |
| **Verification** | Site builds; agent-modified PR/patch applies |
| **Acceptance** | Visible pilot artifact + swarm task completed once |
| **Risk** | Low�Medium |
| **Human review** | Recommended for pilot stack choice |

---

### Package P5 � CI + quality gates

| Field | Value |
|-------|--------|
| **Goal** | Prevent regressions on platform and agents |
| **In scope** | GitHub Actions (or equivalent): lint, unit tests, OpenAPI validate |
| **Out of scope** | E2E cloud deploy |
| **Steps** | 1. Workflow on push/PR. 2. Fail on test/lint failure. |
| **Verification** | Green CI on main |
| **Acceptance** | CI required for merge per CONTRIBUTING |
| **Risk** | Low |
| **Human review** | Optional |

---

### Package P6 � Documentation consolidation (no deletes)

| Field | Value |
|-------|--------|
| **Goal** | Apply �7 without removing PDF |
| **In scope** | `docs/archive/README.md`, move this review to `docs/reviews/`, update root README links |
| **Out of scope** | Deleting PDF |
| **Steps** | 1. Add archive README. 2. Copy `ROADMAP_20260603_153456.md` ? `docs/reviews/`. 3. Link from README. |
| **Verification** | All links resolve |
| **Acceptance** | Single authority chain documented |
| **Risk** | Low |
| **Human review** | Optional |

---

### Package P7 � Identity + registration (Phase 1 start)

| Field | Value |
|-------|--------|
| **Goal** | Ed25519 registration + GitHub owner verification |
| **In scope** | `POST /agents/register`, owner OAuth, agent credentials |
| **Out of scope** | Federation, dashboard |
| **Steps** | Per `ROADMAP.md` �17 Phase 1; ADR 0002 |
| **Verification** | External machine registers and polls |
| **Acceptance** | �Bring your own agent� quickstart works on second machine |
| **Risk** | High |
| **Human review** | **Yes** |

---

### Package P8 � MCP vs REST spike

| Field | Value |
|-------|--------|
| **Goal** | Resolve �16 MCP question |
| **In scope** | Time-boxed prototype, ADR 0003 |
| **Out of scope** | Full MCP server production |
| **Steps** | 1. Map �6.2 ops to MCP tools. 2. Document gaps. 3. Recommend REST-first + MCP adapter or native MCP. |
| **Verification** | ADR merged |
| **Acceptance** | Clear default for SDK (P9) |
| **Risk** | Medium |
| **Human review** | **Yes** |

---

### Package P9 � Agent SDK (Python + TypeScript) � Phase 1

| Field | Value |
|-------|--------|
| **Goal** | �4.3 / �17 Phase 1 SDK |
| **Depends on** | P2, P7, P8 decision |
| **Out of scope** | Credibility ledger |
| **Risk** | Medium |
| **Human review** | Recommended |

---

### Package P10 � Credibility ledger (Phase 2)

| Field | Value |
|-------|--------|
| **Goal** | �9 mechanics with simulated parameters |
| **Depends on** | P2�P3 stable |
| **Steps** | Spec numeric model; simulation tests; then implement |
| **Risk** | High |
| **Human review** | **Yes** |

---

## 9. Suggested Next Prompt For Each Package

### P0 � Repository bootstrap

```text
You are implementing Package P0 for AgentSwarm at m:\Data\Projects\ai_agentSwarm.

Goal: Initialize git and contributor docs only. Do NOT implement platform or agents.

Create:
- .gitignore (Python, Node, OS, .env)
- README.md (one-paragraph vision, status: Phase 0 not started, links to ROADMAP.md)
- CONTRIBUTING.md (PR + human maintainer; note ROADMAP.md �19 applies after Phase 1)
- LICENSE (MIT unless repo owner specifies otherwise � ask if unclear)

Do not commit unless the user asks. After files are written, run: git init; git status.

Acceptance: README accurately states no runnable code yet.
```

### P1 � Monorepo layout + ADR

```text
Package P1: Create monorepo structure for AgentSwarm.

Add directories: platform/, agents/, pilot/news-hub/, docs/adr/, docs/reviews/ (empty placeholders with README stubs ok).

Write docs/adr/0001-phase0-scope.md resolving tension between ROADMAP.md �4.3 (SDK, dashboard, container) and �17 Phase 0 (task pool + 3 agents only). Phase 0 IN: REST task API, audit log, in-process or local three agents. Phase 0 OUT: SDK, dashboard, sandbox, credibility math.

Add docs/status.md with Phase 0 checklist copied from ROADMAP.md �17.

Update root README to link adr and status.

Do not implement platform code yet. No commit unless asked.
```

### P2 � Task pool MVP

```text
Package P2: Implement minimal task pool + signed audit log in platform/.

Follow ROADMAP.md �6.2 endpoints: register (stub ok), poll_tasks, claim, checkpoint, submit, verify flow.

Use SQLite or in-memory store. Python FastAPI OR Node Fastify � pick one and document in docs/adr/0004-stack-choice.md.

Include docs/protocol/openapi.yaml and unit tests for task state machine.

Phase 0: manual agent registration (no GitHub OAuth).

Provide scripts/smoke_task_flow.sh or .ps1 demonstrating create?claim?submit?verify.

Do not add credibility scores. No commit unless asked.
```

### P3 � Reference agents

```text
Package P3: Implement three reference agents in agents/: codewriter, tester, reviewer.

Use a shared agents/common client for poll/claim/sign/submit against local platform from P2.

Implement minimal task handlers (no LLM required): e.g. codewriter patches a file in pilot/news-hub; tester runs pytest; reviewer accepts/rejects.

Ensure one codewriter submission triggers tester then reviewer tasks (implement enqueue rules in platform if missing).

Add integration test or demo script. Verification: demo completes without manual steps.

Out of scope: Docker, LLM APIs, dashboard.
```

### P4 � AI News Hub scaffold

```text
Package P4: Scaffold pilot/news-hub as the Phase 0 target codebase (static site or minimal Next.js � prefer simplest).

Add build instructions in pilot/news-hub/README.md.

Create 2�3 sample tasks compatible with P3 codewriter (small UI/copy changes).

Run end-to-end demo through platform + agents once.

Do not build scrapers or summarizers yet.
```

### P5 � CI

```text
Package P5: Add GitHub Actions workflow .github/workflows/ci.yml running platform and agents tests and OpenAPI validation.

Fail PR on test failure. Document in CONTRIBUTING.md.

Adjust paths to match actual stack chosen in P2.
```

### P6 � Documentation consolidation

```text
Package P6: Documentation consolidation only. Do NOT delete docs/archive/AgentSwarm_Roadmap_final_download.pdf.

1. Add docs/archive/README.md explaining PDF is archived export; ROADMAP.md is authoritative.
2. Copy ROADMAP_20260603_153456.md to docs/reviews/ if not already there.
3. Update root README with docs map per consolidation guide.

No code changes except markdown.
```

### P7 � Identity + registration

```text
Package P7 (Phase 1): Implement agent registration with Ed25519 keys and GitHub OAuth for human owners.

Extend platform POST /agents/register per ROADMAP.md �6.2 and �16 identity notes.

Write docs/adr/0002-identity-model.md.

Add quickstart: external machine registers and polls tasks.

Human review required before merge. Include security notes on key storage.
```

### P8 � MCP vs REST spike

```text
Package P8: Time-boxed spike (max 4 hours wall clock). Compare implementing AgentSwarm task protocol as REST (ROADMAP.md �6.2) vs MCP tools.

Deliver docs/adr/0003-protocol-rest-vs-mcp.md with recommendation: REST-first with MCP adapter, MCP-native, or hybrid.

No production code required; optional minimal MCP prototype allowed.

Do not commit breaking changes to existing REST API without ADR approval.
```

### P9 � Agent SDK

```text
Package P9: Implement reference SDKs packages/sdk-python and packages/sdk-typescript covering register, poll_tasks, claim, checkpoint, submit per OpenAPI from P2.

Follow ADR 0003 protocol decision. Include "bring your own summarizer in 30 minutes" quickstart in docs/quickstart.md.

Tests for signing and request serialization. No credibility ledger.
```

### P10 � Credibility ledger

```text
Package P10 (Phase 2): Specify and implement credibility ledger per ROADMAP.md �9.

First deliver docs/credibility-spec.md with formulas (mint, burn, decay, stake, verifier weighting) and simulation tests.

Then implement ledger storage and API hooks on task verify.

Require human review before merge. Feature-flag credibility in Phase 0 closed swarm until tested.
```

---

## Appendix A � Inventory snapshot (2026-06-03)

| Path | Size (bytes) | Notes |
|------|----------------|-------|
| `ROADMAP.md` | 34,498 | Only product spec |
| `docs/archive/AgentSwarm_Roadmap_final_download.pdf` | 146,078 | Archive; not verified |
| `ROADMAP_20260603_153456.md` | (this file) | Review snapshot |

**SHA256 `ROADMAP.md`:** `3141956DB5DD0E0728A81E45FBBBED597CDF653F15E979A3461ECE240804C61C` (verified 2026-06-03 via `Get-FileHash -Algorithm SHA256`).

---

## Appendix B � Internal roadmap tensions (adversarial)

1. **�Productive documentation� ? progress** � 551 lines, 0 LOC.  
2. **�19 contributing fiction** � references swarm process with no swarm.  
3. **Verifier role vs. types** � glossary �verifier� is cross-cutting; Phase 0 only names tester/reviewer.  
4. **Orchestrator in pilot narrative** (�2) but not in Phase 0 agents � human/orchestrator gap.  
5. **WebSocket mentioned** (�4.3) but protocol shows long-polling only � minor inconsistency.  
6. **Phase 0 �AI News Hub first version�** vs. no `pilot/` directory � goal unsubstantiated.

---

*End of review. This document recommends actions only; no files were deleted or moved during generation.*
